DISCORD_BOT_TOKEN=your_discord_bot_token_here
WHALEBOTS_PATH=C:\Users\DELL\Downloads\WhaleBots_1013

# Multi-PC: bind this bot to one or more channels by ID (comma-separated).
# Leave empty to listen everywhere (single-PC setup).
INSTANCE_CHANNEL=
FLASK_SECRET_KEY=your_random_secret_key_here_change_this_in_production
FLASK_PORT=5000
FLASK_HOST=127.0.0.1
FLASK_DEBUG=True

# Auto-Update: "prompt" asks [y/N] on startup (default N); "off" disables.
AUTO_UPDATE=prompt

